{
  "name": "gdg-tool-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "video:dev": "npm run dev --prefix video_editor",
    "video:render": "npm run render --prefix video_editor",
    "video:check": "npm run check --prefix video_editor"
  }
}
